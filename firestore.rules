rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Fonctions de support
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && 
             exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    // 1. CAPTURES : Lecture publique indispensable pour le classement et le flux en temps réel
    match /catches/{catchId} {
      allow read: if true;
      allow create: if isSignedIn();
      allow update, delete: if isSignedIn() && (isAdmin() || resource.data.userId == request.auth.uid);
    }

    // 2. UTILISATEURS : Lecture autorisée pour tous les membres connectés
    match /users/{userId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && (request.auth.uid == userId || isAdmin());
      allow delete: if isAdmin();
    }

    // 3. ESPÈCES (GUIDE) : Lecture publique, modification Admin
    match /species/{speciesId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // 4. CODES D'INVITATION : Gestion strictement réservée aux Admins
    match /registration_codes/{codeId} {
      allow get: if isSignedIn();
      allow list, write: if isAdmin();
    }

    // 5. COMPÉTITIONS
    match /competitions/{competitionId} {
      allow read: if true;
      allow write: if isAdmin();
    }
  }
}