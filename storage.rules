rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow everyone to read images
      allow read: if true;
      // Allow authenticated users to upload/update/delete images for now (prototyping)
      allow write: if request.auth != null;
    }
  }
}